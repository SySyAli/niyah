# ─── Firebase ─────────────────────────────────────────────────────────────────
# Project ID from Firebase Console: https://console.firebase.google.com/
EXPO_PUBLIC_FIREBASE_PROJECT_ID=your-firebase-project-id

# ─── Google OAuth ────────────────────────────────────────────────────────────
# From Google Cloud Console: https://console.cloud.google.com/apis/credentials
EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID=your-web-client-id.apps.googleusercontent.com
EXPO_PUBLIC_GOOGLE_IOS_CLIENT_ID=your-ios-client-id.apps.googleusercontent.com
EXPO_PUBLIC_GOOGLE_ANDROID_CLIENT_ID=your-android-client-id.apps.googleusercontent.com

# ─── Stripe ──────────────────────────────────────────────────────────────────
# From Stripe Dashboard: https://dashboard.stripe.com/apikeys
# Use pk_test_... for development, pk_live_... for production
# NEVER commit secret keys (sk_...) — those go in Firebase Secret Manager only
EXPO_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key_here

# ─── Firebase Cloud Functions ────────────────────────────────────────────────
# Secret key set via: firebase functions:secrets:set STRIPE_SECRET_KEY
# Webhook secret set via: firebase functions:secrets:set STRIPE_WEBHOOK_SECRET
# (These are never stored in .env — managed by Firebase Secret Manager)

# ─── Firebase Config Files ──────────────────────────────────────────────────
# These files are NOT in the repo. Obtain them from the team or Firebase Console:
#   firebase/GoogleService-Info.plist  (iOS)
#   firebase/google-services.json     (Android)
#
# Download from: Firebase Console → Project Settings → Your Apps
# Place them in the firebase/ directory at the project root.
#
# For EAS cloud builds, upload as file secrets:
#   eas secret:create --scope project --name GOOGLE_SERVICES_PLIST \
#     --type file --value ./firebase/GoogleService-Info.plist
#   eas secret:create --scope project --name GOOGLE_SERVICES_JSON \
#     --type file --value ./firebase/google-services.json
